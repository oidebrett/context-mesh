{
  "name": "back-end",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "dotenv -e ../.env -- concurrently -n ts,api \"npm run dev:watch\" \"npm run dev:start\"",
    "build": "npm run db:generate && tsc -b",
    "dev:watch": "tsc -b -w --preserveWatchOutput",
    "dev:start": "nodemon",
    "start": "dotenv -e ../.env -- npm run db:migrate && tsc -b && npm run local:start",
    "local:start": "dotenv -e ../.env -- node ./dist/app.js",
    "db:generate": "dotenv -e ../.env -- prisma generate",
    "db:migrate": "dotenv -e ../.env -- prisma db push --skip-generate",
    "db:migrate:dev": "dotenv -e ../.env -- prisma migrate dev",
    "db:studio": "dotenv -e ../.env -- prisma studio",
    "debug": "dotenv -e ../.env -- npx prisma db seed && dotenv -e ../.env -- nodemon --inspect=9229 --watch src --exec \"npx tsx\" src/app.ts",
    "start:prod": "dotenv -e ../.env -- node ./dist/app.js"
  },
  "type": "module",
  "dependencies": {
    "@fastify/cors": "9.0.1",
    "@fastify/oauth2": "^7.9.0",
    "@fastify/secure-session": "^6.2.0",
    "@nangohq/node": "^0.69.22",
    "@nangohq/types": "^0.69.22",
    "@prisma/client": "^6.19.0",
    "dotenv-cli": "7.4.1",
    "fastify": "4.26.2",
    "ip-range-check": "^0.2.0",
    "jsonata": "^2.1.0",
    "node-summarizer": "^1.0.7",
    "prisma": "^6.19.0",
    "zod": "3.22.4"
  },
  "nodemonConfig": {
    "watch": [
      "src/"
    ],
    "delay": 500,
    "exec": "dotenv -e ../.env -- npx tsx src/app.ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "prisma": {
    "seed": "dotenv -e ../.env -- node --loader ts-node/esm src/seed_mappings.ts"
  }
}